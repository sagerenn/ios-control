[package]
name = "ios-control-plugin-protocol"
version = "0.1.0"
edition = "2021"

[dependencies]
ios-control-contracts = { path = "../contracts" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
